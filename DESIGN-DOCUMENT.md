
⸻

ClaudeCode 기반 스탠드얼론 워크플로우 자동화 도우미

📌 문서 목적

이 문서는 ClaudeCode 기반의 워크플로우 시각화 및 작업 요청 자동화 프로그램(이하 "ClaudeFlow")을 구현하기 위한 종합 설계서이다. 본 문서는 다음과 같은 목적을 가진다:
	•	플랫폼별 스탠드얼론 실행 가능 (macOS, Windows, Linux)
	•	프로젝트 폴더 내에 기획서 및 리소스와 함께 배치하여 실행
	•	계층형 세션 구조 (Manager → Supervisor → Worker) 자동 생성
	•	사용자 프롬프트 기반 작업 요청 흐름 구성
	•	Claude API와 연동하여 실제 작업 수행
	•	토큰 사용량 및 요금제 상태 시각적 표시

본 문서 하나만으로 독립적인 재구현이 가능할 정도로 상세한 내용을 포함한다.

⸻

⚙️ 기술 사양

✅ 플랫폼 및 프레임워크

영역	기술	설명
실행 환경	Tauri (Rust + Webview)	경량, 보안성, 멀티 플랫폼 빌드 가능
프론트엔드	React + Vite + TailwindCSS + Zustand	빠른 빌드, 상태 관리, 컴포넌트화 UI
백엔드 연동	Tauri Command (Rust)	로컬 파일 접근 및 세션 관리
Claude API	Anthropic Claude (gRPC 또는 REST)	작업 세션 기반 요청 수행

✅ 배포 방식
	•	실행 파일 (claudebuilder.app, .exe, .AppImage)로 빌드
	•	기획서와 함께 프로젝트 폴더에 두고 더블클릭으로 실행 가능


⸻

# ClaudeFlow 개발 계획서

## 핵심 피쳐 마일스톤

1. 스탠드얼론 앱 빌드 및 실행 가능
2. 앱 내에서 현재 경로를 기준으로 복수의 터미널 실행 및 표시 가능
3. 각 터미널에서 ClaudeCode 세션을 실행시킬 수 있음 (사용자의 ClaudeCode 로그인 혹은 API키 정보를 활용하기 위함)
4. 탭 버튼을 눌러서 각 세션의 상태와 정보를 시각화 함
5. 각 터미널에서 상황에 맞는 인자를 넣어 ClaudeCode 세션을 실행시킬 수 있음

6. 각 세션을 스키마로 포장하여 별도의 역할과 상하관계를 정의할 수 있음
7. ClaudeFlow 앱 내 은폐 터미널에서 MCP 서버를 구동시켜 각 세션들과 연동할 수 있음
8. ClaudeCode의 각 세션별 역할 프롬프트를 통해 하위/상위 세션들에게 MCP로 메시지를 전달할 수 있음
9. 기본 역할 프리셋으로 디렉터/매니저/워커 프롬프트 사전 제공. (각 역할별 상세 목표는 후술)
10. 앱에서 MCP 서버들을 등록하여 생성된 세션들에 일괄 연동시켜줄 수 있음.

11. 앱 실행파일과 같은 경로에 기획 문서를 넣고 최초 구동시 "Init" 버튼 활성화 - 작업 계획 설계 진행
12. 사용자의 직접 명령 프롬프트를 입력하고 매니저에게 전달할 수 있는 입력칸 구현
13. 프로젝트 단위의 컨텍스트 기록은 GreeumMCP를 활용하고 내부에 .greeum 폴더를 생성하여 로컬에 기록될 수 있도록 유도.
13. 파일 공유 및 업로드는 내부 .cflow 폴더에서 관리.


* 각 마일스톤 피쳐들은 한 번에 하나씩만 개발을 진행해야 함.
* 각 피쳐에서 추구하는 결과물이 제대로 작동하는지 유닛테스트+검증자동화+사용자검증 까지 거쳐야 다음으로 넘어갈 수 있음
* 각 작업이 완료될 때 마다 GreeumMCP로 작업 내역을 저장할 수 있어야 함.

## 기본 스키마

### MilestoneSchema

- (개요) Director 세션이 하위 Manager 세션에게 전달하는 용도
- 필수 항목:
    - 완료되어야 하는 선행 작업 목록
    - 현재 작업을 진행하기 위해 참고해야 하는 파일 혹은 폴더의 경로 목록
    - 작업이 완료되었음을 검증하기 위한 필수 체크리스트
    - 이외에 작업에 필요한 기타 정보 등

### WorkSchema

- (개요) Manager 세션이 하위 Worker 세션에게 전달하는 용도
- 필수 항목:
    - 현재 작업이 필요한 이유
    - 작업을 위해 참고해야 하는 파일 혹은 폴더의 경로 목록
    - 현재 작업이 완료된 후 기대되는 결과
    - 현재 작업이 완료된 후 검증되어야 하는 루틴
    - 이외에 작업에 필요한 기타 정보 등

## 기본 역할 프리셋

### Director

- 한 프로젝트에 하나의 세션만 존재 가능함.
- 직접 코드 작성 등의 작업은 불가능함.
- 사용자와 직접 상호작용 하는 유일한 세션임.
- 사용자의 지시사항을 받아서 큰 기능 단위의 마일스톤을 구성해야 함.
- 구성된 마일스톤을 바탕으로 업무의 선후 관계를 바탕으로 DESIGN-DOCUMENT.md, To-do-list.md와 Production-check-list.md를 생성
- 각 마일스톤 작업에 대해 하위 Manager 세션을 생성.
- 생성된 각 Manager 세션에 대해 MilestoneSchema를 전달.
- 임의의 Manager 세션에 대해 작업이 완료된 것을 확인받으면 검증 결과까지 파악한 후 만족스러우면 해당 작업을 선행 작업으로 두고 있는 후속 작업들 중 모든 선행작업이 완료되었지만 작업이 개시되지 않은 Manager 세션이 있는 경우 작업 결과를 전달 및 작업 시작 지시.


### Manager

- 동시에 여러 세션 존재 가능.
- 완료된 작업 사항에 대한 자동화된 검증 및 유닛테스트에 대해서만 코드 작성이 가능함.
- Director 세션에게서 MilestoneSchema를 전달받고 구체적이고 세부적인 업무 단위로 분할.
- 각 하위 업무들은 한 번에 파일 하나만 수정할 수 있도록 분할되어야 하며, 어떤 변수를 선언하고 어떤 함수가 어떤 인자를 받아서 어떤 처리를 한 후 어떤 결과를 반환해야 하는지 명세되어 안내되어야 함.
- 하나의 업무에 대해 하나의 Worker가 생성됨.
- 각 Worker에게 하나의 WorkSchema 형태로 업무가 배정됨.
- 모든 하위 Worker가 작업이 완료되었다면 전체 품질 검증을 진행.
- 검증 및 테스트를 통과하지 못한 영역이 있다면 해당 부분에 대해 작업을 롤백시키고 문제상황에 대한 개선된 대책이 반영된 새 WorkSchema를 새 Worker 생성하여 전달. 이후 반복.
- 모든 하위 Worker 작업에 대해 품질 검증까지 완료되면 Director에게 작업 완료 사실을 알림.

### Worker

- 동시에 여러 섹션 존재 가능.
- 파일 하나에 대한 작성 및 수정 권한을 가짐.
- 프로젝트 내의 모든 연관된 파일 및 폴더에 대한 읽기 권한을 가짐.
- 세부 하위 업무에 대해 쉽게 생성되며 쉽게 제거되는 세션임.
- 상위 Manager 세션에게서 전달받은 WorkSchema를 바탕으로 업무 진행.
- 업무가 완료된 후 완료된 작업에 대해 컴파일 및 린터 에러 등 최소한의 코드 리뷰 및 검증 책임은 Worker에게 있음.
- 코드 작성 및 수정이 완료되면 Manager 세션에게 완료 사항을 전달 후 세션 자동 파기.

⸻

🖥 UI 구성

[좌측] 트리 시각화 영역 (넓음, 약 70%)
	•	Manager → Supervisors → Workers 계층적 노드
	•	각 노드는 상태에 따라 색상/아이콘 변경
	•	마우스 hover 시 개요 툴팁, 클릭 시 상세 패널

[우측] 작업 제어 및 정보 표시 패널 (좁음, 약 30%)
	•	프롬프트 입력창 (최초 요청 시)
	•	현재 세션 수, 진행률, 토큰 사용량, 요금제 상태
	•	결과물 요약 / 열람 버튼
	•	세션 강제 종료 / 재시도 기능

⸻

⸻

📈 기대 결과 및 사용성

요소	기대 결과
사용성	기획서 + 실행파일만으로 작업 요청 환경 구축 가능
시각화	세션 흐름을 한 눈에 트리로 이해 가능
API 추적	토큰 사용량과 요금제에 따른 가시적 사용 패턴 제공
재현성	동일한 기획서 + 요청 입력으로 반복 실행 가능


⸻

✅ 구현 우선순위 요약
	1.	실행파일 기반 Tauri 앱 구성
	2.  	앱에서 독립 ClaudeCode세션 생성
	3.  	각 ClaudeCode에 기본 역할 프롬프트를 고정
	4.	앱 내부 기능에 대한 MCP 엔드포인트 구성 (세션 생성/할당/제거, 타 세션에 메시지 전달, 공용 메모리 읽기/쓰기)
	5.	트리 기반 시각화 UI 구성
	6.	토큰 및 요금제 시각적 정보 표시

⸻

이 설계서는 ClaudeCode 기반 ClaudeFlow MVP를 구현하기 위한 기준 문서로 사용되며, 이 문서 하나만으로 전체 시스템을 이해하고 구현할 수 있어야 한다.
